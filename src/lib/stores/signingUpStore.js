import { readable, writable } from 'svelte/store';

export const avatar_cropped_blob_url = writable(null);
export const avatar_cropped = writable(null);
export const avatar_original = writable(null);
